# Python
__pycache__/
*.py[oc]
*.pyc
build/
dist/
wheels/
*.egg-info
.pytest_cache/

# Virtual env
.venv

# Piper voice models (legacy - no longer used with Kokoro)
voices/*.onnx
voices/*.onnx.json

# Daemon socket
*.sock

# MemPalace per-project files
mempalace.yaml
entities.json

# OpenCode instruction file (AI agent context, not project code)
AGENTS.md

# SpeckIt project specification (volatile per-developer state)
.specify/
opencode.json

# Feature specification (volatile per-developer state)
specs/
docs/
poc_screen.py
